document.getElementById('loginForm').addEventListener('submit',e=>{e.preventDefault();const u=document.getElementById('username').value.trim();const p=document.getElementById('password').value;const user=USERS[u];if(!user||user.pass!==p)return notifyError('Sai tài khoản hoặc mật khẩu. Kiểm tra lại tài khoản được cấp.','Đăng nhập thất bại');localStorage.setItem('repairUser',JSON.stringify({username:u,role:user.role,name:user.name}));location.href='dashboard.html'});
