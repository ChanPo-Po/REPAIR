# V12.6.1 – Sửa lỗi textContent

- Giữ nguyên dashboard.html do người dùng đã chỉnh và xóa các phần không cần.
- Sửa `openTab()` để không lỗi khi `pageTitle` hoặc `pageSubtitle` không còn trong HTML.
- Bump cache lên `12.6.1`.
